import React from 'react';

const PostComment = () => {
  return (
    <button name="submit" type="submit" className="btn btn-gradient">
      Post Comment
    </button>
  );
};

export default PostComment;
